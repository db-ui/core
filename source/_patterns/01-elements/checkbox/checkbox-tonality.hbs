{{#each tonality as |tonality| }}
	<!-- [html-validate-disable-next heading-level -- the h1 is already provided by the general pattern] -->
	<h2>{{ tonality }}</h2>
	<p data-tonality="{{ tonality }}">
		{{> elements-checkbox id=(unique-id) label="Checkbox" }}
	</p>
{{/each }}
